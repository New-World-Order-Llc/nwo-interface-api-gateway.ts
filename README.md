# nwo-interface-api-gateway.ts
Provides deterministic read‑only API access to projections. Validates query envelopes, routes requests through contract‑defined handlers, and returns reproducible projection snapshots. Ensures audit‑aligned, nondestructive external access to Beast System 3.0 state.
